apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: postman-web-ingress
spec:
  rules:
  - host: postman.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: postman-web
            port:
              number: 80
