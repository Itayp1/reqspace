apiVersion: v1
kind: Service
metadata:
  name: reqspace-web
spec:
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  selector:
    app: reqspace-web
  ports:
  - port: 80
    targetPort: 3005
  type: ClusterIP
