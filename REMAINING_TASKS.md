# רשימת המשימות הפתוחות (Remaining Tasks)

מסמך זה מרכז את כל המשימות שנשארו פתוחות בקובץ ה-`TODO.md` המקורי, לאחר סינון משימות שכבר בוצעו או בוטלו. לכל משימה מצורפת הערכת מאמץ (Size) והערכה משוערת לכמות הטוקנים שיידרשו למודל ה-AI כדי להשלים אותה בהצלחה (כולל קריאת קוד, כתיבה, טסטים ותיקונים).

**עדכון אחרון:** תוקנו כל הכישלונות שתועדו ב-`failed.md` (כולל בעיות חיבור ב-E2E, שגיאות מסד נתונים וחסרי ולידציה) ונוקו כל סקריפטי העזר הזמניים.

## ביצועים וסקייל (PERF)

| מס' | מזהה מקורי | תיאור המשימה | מאמץ | עלות טוקנים (משוערת) |
|---|---|---|---|---|
| 8 | PERF-2 | Lazy-load לעץ התיקיות בלקוח (טעינת ילדים רק בפתיחה). תלוי ב-PERF-1. | L | ~30,000 |
| 9 | PERF-3 | החלפת הפגינציה מבוססת Offset ל-Cursor Pagination בכל הרשימות במערכת. | L | ~30,000 |
| 11 | PERF-5 | הטמעת מנגנון Cache קצר מועד לבדיקות הרשאות והגדרות מערכת. | M | ~15,000 |
| 12 | PERF-6 | צמצום המידע שנשלח ב-API (Summary בלבד במקום פירוט מלא + דחיסת GZIP). | M | ~12,000 |
| 13 | PERF-7 | אופטימיזציה לגודל ה-Bundle של אפליקציית הלקוח (החלפת ספריות שוקלות, Lazy loading). | M | ~15,000 |

## כיסוי בדיקות (TEST)

| מס' | מזהה מקורי | תיאור המשימה | מאמץ | עלות טוקנים (משוערת) |
|---|---|---|---|---|
| [x] 21 | TEST-4 | כיסוי מלא לשכבת ההרשאות ב-API (לעשות זאת יד ביד עם משימות ה-SEC). | L | ~30,000 |
| 23 | TEST-6 | הוספת תקציבי ביצועים (Performance budgets) לבדיקות שירוצו ב-CI. | M | ~15,000 |
