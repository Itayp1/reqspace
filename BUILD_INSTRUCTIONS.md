# הנחיות בניה ופריסה (Build & Deployment)

קובץ זה מתעד את צורת הפריסה וההרצה של הפרויקט (Postman Clone) בסביבת Production וכיצד לטפל בבנייה (Build).

## 1. פריסה אוטומטית מקומית באמצעות PM2 (מצב נוכחי)

הפרויקט מוגדר לרוץ בעזרת **PM2** עם קובץ `ecosystem.config.js`. 
במצב זה, **הבנייה (Build) מתבצעת אוטומטית** ברגע שיש שינוי בקוד (ה-PM2 מאזין לשינויים).

- **התהליך שרץ מאחורי הקלעים:**
  כאשר מריצים את ה-PM2, הוא מפעיל את הסקריפט `deploy.js`. הסקריפט עושה את הפעולות הבאות ברצף:
  1. בונה את הלקוח: `npm run build` בתוך תיקיית `client`.
  2. בונה את השרת: `npm run build` (TypeScript) בתוך תיקיית `server`.
  3. מעלה את השרת על פורט 3005.

### פקודות שימושיות ל-PM2:
* **צפייה בלוגים בזמן אמת:** `pm2 logs postman-clone`
* **הפעלה מחדש (Restart):** `pm2 restart postman-clone`
* **עצירת השרת:** `pm2 stop postman-clone`
* **רשימת כל השרתים הרצים:** `pm2 list`

---

## 2. בנייה ידנית (Manual Build)

אם אתה רוצה לארוז את הפרויקט ידנית (לדוגמה, להעלות לשרת אמיתי או להריץ בלי PM2), עליך לבצע את השלבים הבאים:

1. **בניית ה-Client (React):**
   ```bash
   cd client
   npm install
   npm run build
   ```
   *פעולה זו תיצור את תיקיית `client/dist` עם קבצי ה-Frontend המוכנים.*

2. **בניית ה-Server (Node.js/Express):**
   ```bash
   cd ../server
   npm install
   npm run build
   ```
   *פעולה זו תקמפל את קוד ה-TypeScript לתוך תיקיית `server/dist`.*

3. **הרצת השרת ב-Production:**
   בתוך תיקיית `server`, ודא שיש לך קובץ `.env` מוגדר היטב (עם חיבור ל-DB), ואז הרץ:
   ```bash
   NODE_ENV=production node dist/index.js
   ```
   *השרת באופן אוטומטי יגיש גם את קבצי ה-Client מתוך ה-API ויעבוד כיחידה אחת שלמה.*

---

## 3. פריסה באמצעות Docker (אופציונלי)

במידה ותרצה לפרוס את הפרויקט על גבי שרת מרוחק או סביבת ענן בעזרת קונטיינרים:

בפרויקט כבר קיים קובץ `Dockerfile` ו-`docker-compose.yml`.
ניתן לבנות ולהרים הכל (כולל ה-DB המקומי) בעזרת הפקודה:
```bash
docker-compose up --build -d
```
זה יבצע את תהליך ה-Build האוטומטי (לקוח + שרת) ישירות בתוך ה-Docker ויחשוף את הפרויקט.
